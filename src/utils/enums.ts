export enum ATTACK_TYPES {
  ROCK = "rock",
  PAPER = "paper",
  SCISSORS = "scissors",
  RANDOM = "random",
}

export enum PLAYER_TYPES {
  USER,
  OPPONENT,
  OTHER
}

export enum ROUND_RESULT {
  WIN = "win",
  LOSE = "lose",
  DRAW = "draw",
}